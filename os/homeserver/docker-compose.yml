name: lab

networks:
  traefik:
    driver: bridge
    ipam:
      config:
        - subnet: 172.18.0.0/24
  pihole_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.31.0.0/16
  dozzle_agent:
    driver: bridge
    internal: true

volumes:
  model-cache:
  immich-db:
  immich-redis:

secrets:
  cloudflare_dns_api_token:
    file: ./secrets/cloudflare_dns_api_token
  traefik_users:
    file: ./secrets/traefik-users
  mqtt-passwd:
    file: ./secrets/mqtt-passwd
  mqtt-acl:
    file: ./secrets/mqtt-acl

services:
  # Traefik Reverse Proxy
  traefik:
    image: traefik@sha256:4df0a50fcf71b454c0d7ad17675776dc8d37359deae3291895bdaa008c1b9972
    container_name: traefik
    restart: unless-stopped
    environment:
      - DOMAIN=${DOMAIN:?set DOMAIN=n33lab.com}
      - CF_DNS_API_TOKEN_FILE=/run/secrets/cloudflare_dns_api_token
    ports:
      - "80:80" # HTTP
      - "443:443" # HTTPS
      - "192.168.1.33:8883:8883"
    command:
      - --api.dashboard=true
      - --providers.docker=false
      - --providers.file.directory=/etc/traefik/dynamic
      - --providers.file.watch=true
      - --entrypoints.websecure.transport.respondingTimeouts.readTimeout=0
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --entrypoints.mqtts.address=:8883
      - --certificatesresolvers.cloudflare.acme.email=${ACME_EMAIL:?set ACME_EMAIL}
      - --certificatesresolvers.cloudflare.acme.storage=${ACME_STORAGE:-/letsencrypt/acme.json}
      - --certificatesresolvers.cloudflare.acme.caserver=${ACME_CA_SERVER:-https://acme-v02.api.letsencrypt.org/directory}
      - --certificatesresolvers.cloudflare.acme.dnschallenge.provider=cloudflare
      - --certificatesresolvers.cloudflare.acme.dnschallenge.resolvers=1.1.1.1:53,1.0.0.1:53
    volumes:
      - ./config/traefik/dynamic:/etc/traefik/dynamic:ro
      - ./data/traefik/letsencrypt:/letsencrypt
    secrets:
      - cloudflare_dns_api_token
      - source: traefik_users
        target: traefik-users
    networks:
      - traefik
    extra_hosts:
      - "host.docker.internal:host-gateway"
  # Glance Dashboard
  glance:
    image: glanceapp/glance@sha256:32ab73d80f2b8b5fb0735b0431deb36b93fbb6b2fb43592449b0178c8b83e350
    container_name: glance
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
      - DOMAIN=${DOMAIN:?set DOMAIN=n33lab.com}
      - PIHOLE_PASSWORD=${PIHOLE_PASSWORD:?set in .env}
    volumes:
      - ./config/glance:/app/config:ro
    networks:
      - traefik
    extra_hosts:
      - "host.docker.internal:host-gateway"
  # Dockhand - Docker Management UI
  dockhand:
    image: fnsys/dockhand@sha256:29d4183d8aef2cc7fbc50750fa11434ec331e044647288ae47fe15260e68a44c
    container_name: dockhand
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./data/dockhand:/data
    networks:
      - traefik
  # Tailscale - VPN Access
  tailscale:
    image: tailscale/tailscale@sha256:25cde9ad76020b0e29229136d0c38b5962e9a0e1774ffac9b0df68e4a37d6cf0
    container_name: tailscale
    hostname: homeserver
    restart: unless-stopped
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY}
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
      - TS_ROUTES=172.18.0.0/24,172.31.0.0/16,192.168.1.0/24
      - TS_ACCEPT_DNS=false
    volumes:
      - ./data/tailscale:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    networks:
      - traefik
      - pihole_network

  # Pi-hole DNS Server
  pihole:
    image: pihole/pihole@sha256:f7d1be836e3bc608b56d82fc9904f5a831cdfbc0dc9c6d58f94e4c985c70038b
    container_name: pihole
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
      - FTLCONF_webserver_api_password=${PIHOLE_PASSWORD:?set in .env}
      - FTLCONF_dns_upstreams=${PIHOLE_DNS:-1.1.1.1;1.0.0.1}
      - FTLCONF_dns_listeningMode=ALL
      - FTLCONF_dns_dnssec=true
      - FTLCONF_webserver_interface_theme=default-dark
      - FTLCONF_webserver_domain=pihole.${DOMAIN:?set DOMAIN=n33lab.com}
      - FTLCONF_misc_dnsmasq_lines=address=/n33lab.com/192.168.1.33
    volumes:
      - ./config/pihole/etc-pihole:/etc/pihole
    ports:
      - "192.168.1.33:53:53/tcp"
      - "192.168.1.33:53:53/udp"
    dns:
      - 127.0.0.1
      - 1.1.1.1
    cap_add:
      - NET_ADMIN
    networks:
      traefik: {}
      pihole_network:
        ipv4_address: "172.31.0.100"
    labels:
      - "homepage.group=Network"
      - "homepage.name=Pi-hole"
      - "homepage.icon=pihole.png"
      - "homepage.href=https://pihole.${DOMAIN:?set DOMAIN=n33lab.com}/admin"
      - "homepage.description=Network-wide ad blocking and DNS"
      - "homepage.weight=1"
      - "homepage.widget.type=pihole"
      - "homepage.widget.url=http://pihole"
      - "homepage.widget.key=${PIHOLE_API_KEY:-}"

  # DHCP Helper for Pi-hole
  dhcphelper:
    image: noamokman/dhcp-helper@sha256:25a33e63a20f7ec06465653c40ccde3f692a23074d566ee9dcbb11d75307d538
    container_name: dhcp-helper
    restart: unless-stopped
    network_mode: "host"
    command: -s 172.31.0.100
    cap_add:
      - NET_ADMIN

  # qBittorrent Download Client
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent@sha256:b024436f8ca665d16d9a997d26fd27fdf867ee5566ba09f32764e7b2976d3e02
    container_name: qbittorrent
    restart: unless-stopped
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Europe/Madrid}
      - WEBUI_PORT=8080
    volumes:
      - ./config/qbittorrent:/config
      - ${DATA_ROOT:-./data/shared}:/data
    ports:
      - "6881:6881"
      - "6881:6881/udp"
    networks:
      - traefik
  # Immich Photo Management
  immich-server:
    image: ghcr.io/immich-app/immich-server@sha256:46dedfc5848f7313bd6b584ea9f2648057430307aad6de56de968f6710a72cae
    container_name: immich_server
    restart: unless-stopped
    volumes:
      - ${UPLOAD_LOCATION:-./data/immich}:/data
      - /etc/localtime:/etc/localtime:ro
      - /home/andrew/immich:/mnt/old-immich:ro
      - /home/andrew/immich-test:/mnt/immich-test:ro
    environment:
      - DB_HOSTNAME=database
    env_file:
      - ./secrets/immich-server.env
    depends_on:
      - redis
      - database
    networks:
      - traefik
    healthcheck:
      disable: false

  # Immich Machine Learning
  immich-machine-learning:
    image: ghcr.io/immich-app/immich-machine-learning@sha256:b3deefd1826f113824e9d7bc30d905e7f823535887d03f869330946b6db3b44a
    container_name: immich_machine_learning
    restart: unless-stopped
    volumes:
      - model-cache:/cache
    env_file:
      - ./secrets/immich-ml.env
    networks:
      - traefik
    healthcheck:
      disable: false

  # Immich Redis Cache
  redis:
    image: docker.io/valkey/valkey:8-bookworm@sha256:fea8b3e67b15729d4bb70589eb03367bab9ad1ee89c876f54327fc7c6e618571
    container_name: immich_redis
    restart: unless-stopped
    volumes:
      - immich-redis:/data
    networks:
      - traefik
    healthcheck:
      test: redis-cli ping || exit 1

  # Immich PostgreSQL Database
  database:
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    container_name: immich_postgres
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD:?set in .env}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_DB: ${DB_DATABASE_NAME:-immich}
      POSTGRES_INITDB_ARGS: "--data-checksums"
    volumes:
      - immich-db:/var/lib/postgresql/data
    shm_size: 128mb
    networks:
      - traefik

  # Open WebUI for AI
  openwebui:
    image: ghcr.io/open-webui/open-webui@sha256:6a773e5c3a246b65cbe74ce942b294292c0e5f81c138f703d111bc162f7d7c3d
    container_name: openwebui
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - ./data/openwebui:/app/backend/data
    networks:
      - traefik
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Excalidraw Collaborative Whiteboard
  excalidraw:
    image: excalidraw/excalidraw@sha256:f7ee194addd607bf831d2af0f0a34463dd4225e426cf35199ef0b12a803398e9
    container_name: excalidraw
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
    networks:
      - traefik
  # Gatus Monitoring
  gatus:
    image: ghcr.io/twin/gatus@sha256:c5f210d095fa78e6efaa20ffeb14803f2ba4f10615e16a6d12087697149617f0
    container_name: gatus
    restart: unless-stopped
    env_file:
      - ./secrets/gatus.env
    volumes:
      - ./config/gatus:/config:ro
      - ./data/gatus:/var/lib/gatus
    networks:
      - traefik
    extra_hosts:
      - "host.docker.internal:host-gateway"
  # Dozzle - Real-time Log Viewer
  dozzle:
    image: amir20/dozzle@sha256:1c1060cfb5402093c4e0f03f3534d7deaffeb0a6f6dd034e7c5f244603f35fb3
    container_name: dozzle
    restart: unless-stopped
    environment:
      - DOZZLE_REMOTE_AGENT=dozzle-agent:7007
    networks:
      - traefik
      - dozzle_agent
  dozzle-agent:
    image: amir20/dozzle@sha256:1c1060cfb5402093c4e0f03f3534d7deaffeb0a6f6dd034e7c5f244603f35fb3
    container_name: dozzle-agent
    restart: unless-stopped
    command: agent
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - dozzle_agent
  # FileBrowser Quantum - File Server Web UI
  filebrowser:
    image: gtstef/filebrowser@sha256:fc213590ebc090cb8205125cd3ecdfa9066b9ed4a0b53fe2570972d6a3379e73
    container_name: filebrowser
    user: filebrowser
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - ./data/filebrowser:/home/filebrowser/data
      - ./config/filebrowser/config.yaml:/home/filebrowser/data/config.yaml:ro
      - ${DATA_ROOT:-./data/shared}:/folder
    networks:
      - traefik
  # Backrest - Restic Backup UI
  backrest:
    image: garethgeorge/backrest@sha256:b852979754281026230cc69fb11428e6d57c9a97784ab4a444ffc7934c53a215
    container_name: backrest
    restart: unless-stopped
    environment:
      - BACKREST_DATA=/data
      - BACKREST_CONFIG=/config/config.json
      - XDG_CACHE_HOME=/cache
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - ./data/backrest/data:/data
      - ./data/backrest/config:/config
      - ./data/backrest/cache:/cache
      - /volume2/backups:/repos
      - ./data:/userdata:ro
    networks:
      - traefik
  # Home Assistant - Smart Home Hub
  # Host network is required for local device auto-discovery (mDNS/zeroconf,
  # Chromecast, HomeKit). Because host-networked containers can't be on the
  # traefik bridge, Traefik routes to it via the file provider instead of
  # labels (see config/traefik/dynamic/homeassistant.yml).
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant@sha256:5a531753cea96444200158fc2b0ac7ccd739291ec50414877b396de6e0bb29b3
    container_name: homeassistant
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config/homeassistant:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ:-Europe/Madrid}
  # Mosquitto MQTT Broker (message bus for Node-RED, ESPHome, Home Assistant)
  mosquitto:
    image: eclipse-mosquitto@sha256:6f8d8a947c506f8a2290ec65cd4bd2bc7cb4d43fb5f6271f861cb013e2ef9797
    container_name: mosquitto
    restart: unless-stopped
    user: root
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        cp /run/secrets/mqtt-passwd /tmp/mqtt-passwd &&
        cp /run/secrets/mqtt-acl /tmp/mqtt-acl &&
        chmod 600 /tmp/mqtt-passwd /tmp/mqtt-acl &&
        chown 1883:1883 /tmp/mqtt-passwd /tmp/mqtt-acl &&
        exec mosquitto -c /mosquitto/config/mosquitto.conf
    environment:
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - ./config/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./data/mosquitto/data:/mosquitto/data
    secrets:
      - mqtt-passwd
      - mqtt-acl
    networks:
      - traefik
  # Node-RED - Visual Automation Flows
  node-red:
    image: nodered/node-red@sha256:10f40d0a83e7e5852b13d4d472b2006b05b1cca6d55e2f29a55a12c25a630cb6
    container_name: node-red
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - ./data/node-red:/data
    networks:
      - traefik
  # ESPHome - Manage ESP32/ESP8266 devices
  # Host network for device discovery + OTA. Traefik routes via the file
  # provider instead of labels (see config/traefik/dynamic/esphome.yml).
  esphome:
    image: ghcr.io/esphome/esphome@sha256:de90d689b89e20f171b0fbdd0dfea31b21ef647aa7e83d6efd2916ca6e7a30d2
    container_name: esphome
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config/esphome:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=${TZ:-Europe/Madrid}
    env_file:
      - ./secrets/esphome.env
  # Music Assistant - Whole-home audio
  # Host network for player discovery + streaming. Traefik routes via the file
  # provider instead of labels (see config/traefik/dynamic/musicassistant.yml).
  music-assistant:
    image: ghcr.io/music-assistant/server@sha256:5500c53c5129bbabfb0de2c2c298fd0ef15fe34207bbe5794f49c587b76bde95
    container_name: music-assistant
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data/music-assistant:/data
    environment:
      - TZ=${TZ:-Europe/Madrid}
  # Forgejo - Self-hosted Git (GitHub private repos migration)
  forgejo:
    image: codeberg.org/forgejo/forgejo@sha256:dbb0f88677f0c65cd1b66fb83504225aa5a04c4bc4a5ffdf9fc9a3a6d5bb1c68
    container_name: forgejo
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Europe/Madrid}
      - USER_UID=${PUID:-1000}
      - USER_GID=${PGID:-1000}
      - FORGEJO__server__DOMAIN=git.${DOMAIN:?set DOMAIN=n33lab.com}
      - FORGEJO__server__ROOT_URL=https://git.${DOMAIN:?set DOMAIN=n33lab.com}/
      - FORGEJO__server__SSH_DOMAIN=git.${DOMAIN:?set DOMAIN=n33lab.com}
      - FORGEJO__server__SSH_PORT=222
      - FORGEJO__attachment__ALLOWED_TYPES=image/jpeg|image/png|application/zip|application/gzip|application/octet-stream
      - FORGEJO__attachment__MAX_SIZE=512
      - FORGEJO__database__DB_TYPE=sqlite3
      - FORGEJO__security__INSTALL_LOCK=true
      - FORGEJO__service__DISABLE_REGISTRATION=true
      - FORGEJO__migrations__ALLOWED_DOMAINS=github.com,*.github.com
    volumes:
      - ./data/forgejo:/data
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "222:22"
    networks:
      - traefik
