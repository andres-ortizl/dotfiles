[Unit]
Description=awww wallpaper daemon
PartOf=hyprland-session.target

[Service]
Type=exec
ExecStart=/usr/bin/awww-daemon
ExecStartPost=/bin/bash -c 'sleep 0.5 && %h/.config/hypr/scripts/setwall startup'
Restart=on-failure
RestartSec=2
